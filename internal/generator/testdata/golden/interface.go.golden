// Code generated by xll-gen golden. DO NOT EDIT.
package generated

import (
	"context"
	"time"
	"github.com/xll-gen/types/go/protocol"
	"github.com/xll-gen/xll-gen/pkg/server"
)

// Force usage of protocol to avoid unused import error
var _ = protocol.Bool{}

type XllService interface {
		SyncStr(ctx context.Context, s string) (string, error)
		SyncInt(ctx context.Context, i int32) (int32, error)
		SyncFloat(ctx context.Context, f float64) (float64, error)
		SyncBool(ctx context.Context, b bool) (bool, error)
		SyncAny(ctx context.Context, a *protocol.Any) (any, error)
		SyncGrid(ctx context.Context, g *protocol.Grid) ([][]any, error)
		SyncNumGrid(ctx context.Context, ng *protocol.NumGrid) ([][]float64, error)
		SyncRange(ctx context.Context, r *protocol.Range) (int32, error)
		SyncDate(ctx context.Context, d time.Time) (int32, error)
		SyncMulti(ctx context.Context, s string, i int32, f float64, b bool, g *protocol.Grid) (string, error)
		SyncCachedGrid(ctx context.Context, k string) ([][]any, error)
		CallerMacroRange(ctx context.Context, r *protocol.Range, caller *protocol.Range) (int32, error)
		AsyncStr(ctx context.Context, s string) (string, error)
		AsyncInt(ctx context.Context, i int32) (int32, error)
		AsyncGrid(ctx context.Context, g *protocol.Grid) ([][]any, error)
		AsyncNumGrid(ctx context.Context, ng *protocol.NumGrid) ([][]float64, error)
		AsyncAny(ctx context.Context, a *protocol.Any) (any, error)
		RtdScalars_RTD(ctx context.Context, topicID int32, i int32, s string, f float64) error
			RtdComposite_RTD(ctx context.Context, topicID int32, g *protocol.Grid, ng *protocol.NumGrid) error
			RtdRangeAny_RTD(ctx context.Context, topicID int32, r *protocol.Range, a *protocol.Any) error
			OnceScalar(ctx context.Context, i int32, f float64) (float64, error)
		OnceGrid(ctx context.Context, s string) ([][]any, error)
		OnceNumGrid(ctx context.Context, s string) ([][]float64, error)
		OnceComposite(ctx context.Context, g *protocol.Grid, ng *protocol.NumGrid, r *protocol.Range, a *protocol.Any) (any, error)
		OnceMemoize(ctx context.Context, i int32) (float64, error)
		OnceTTL(ctx context.Context, i int32) (float64, error)

	OnRecalc(ctx context.Context) error

	RunReport(ctx context.Context, cmd server.CommandContext) error


	// OnCalculationCanceled runs when the user interrupts a recalculation (Esc).
	//
	// It only fires if xll.yaml declares `- type: CalculationCanceled` under
	// events; otherwise this method is required by the interface but never
	// called.
	//
	// IMPORTANT — a cancelled cycle fires BOTH handlers. Excel emits
	// CalculationCanceled and then CalculationEnded 2-6 ms later, so
	// OnCalculationCanceled is followed by OnCalculationEnded for the SAME
	// interrupted cycle, in that order (the ordering is guaranteed: the XLL
	// blocks Excel's UI thread until this handler returns). Do not assume
	// "cancelled means Ended will not come".
	//
	// Cancellation clears nothing: per-cycle caches and any commands you
	// scheduled are handled by the CalculationEnded path that follows, so
	// ScheduleSet/ScheduleFormat issued here are still applied.
	//
	// Like OnCalculationEnded, this runs while Excel's UI thread is blocked —
	// never drive Excel over COM from it; use ScheduleSet/ScheduleFormat.
	OnCalculationCanceled(ctx context.Context) error


	OnRtdConnect(ctx context.Context, topicID int32, strings []string, newValues bool) error
	OnRtdDisconnect(ctx context.Context, topicID int32) error

}
