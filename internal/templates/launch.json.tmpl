{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Excel (C++)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "{{ .ExcelPath }}",
            "args": [
                "${workspaceFolder}/build/{{ .ProjectName }}.xll"
            ],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "externalTerminal"
        },
        {
            "name": "Attach to Go Server",
            "type": "go",
            "request": "attach",
            "mode": "local",
            "processId": "${command:pickProcess}"
        }
    ],
    "compounds": [
        {
            "name": "Debug All",
            "configurations": ["Debug Excel (C++)", "Attach to Go Server"]
        }
    ]
}
