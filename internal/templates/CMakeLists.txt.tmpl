# Code generated by xll-gen {{.Version}}. DO NOT EDIT.
cmake_minimum_required(VERSION 3.28)

# ==============================================================================
# Architecture lock: Windows x64 (amd64) ONLY (AGENTS.md §0.1).
# ==============================================================================
# xll-gen has NO 32-bit build: the shm Go guest's Win32 struct layouts are
# amd64-only, and a 32-bit XLL cannot load into 64-bit Excel. A configure that
# slips to 32-bit (e.g. a stock 32-bit MSVC toolset defaulting a Visual Studio
# generator to Win32) would produce an add-in that mismatches the amd64 Go
# server. Two complementary guards below cover both toolchain families:
#
#   1. MSVC / Visual Studio (multi-config, platform selectable at configure
#      time): default the generator PLATFORM to x64 unless the user explicitly
#      passed -A. Must run BEFORE project(). Guarded on the generator name so it
#      is a no-op for MinGW Makefiles / Ninja (which reject platform specs — the
#      toolchain, not this var, fixes their bitness; the pointer-size check
#      below is their guard).
#   2. ALL toolchains (MinGW x86_64 reference build included): a post-project()
#      pointer-size assert that hard-fails any 32-bit target.
if(CMAKE_GENERATOR MATCHES "Visual Studio" AND NOT CMAKE_GENERATOR_PLATFORM)
    set(CMAKE_GENERATOR_PLATFORM "x64")
endif()

project({{ .ProjectName }} LANGUAGES C CXX)

# Universal 64-bit gate: CMAKE_SIZEOF_VOID_P is the actual target pointer width
# after the compiler probe (8 = x64, required; 4 = 32-bit x86, unsupported).
# Covers MinGW x86 toolchains, a 32-bit MSVC toolset, and a VS generator forced
# to Win32 — none of which the platform default above can catch on its own.
if(NOT CMAKE_SIZEOF_VOID_P EQUAL 8)
    message(FATAL_ERROR
        "xll-gen requires a 64-bit (x64/amd64) toolchain, but this configure is "
        "targeting a ${CMAKE_SIZEOF_VOID_P}-byte-pointer (32-bit) build. "
        "32-bit x86 is unsupported (AGENTS.md §0.1): the shm Go guest is amd64-only "
        "and a 32-bit XLL cannot load into 64-bit Excel. "
        "Use a 64-bit toolchain (MSVC: -A x64; MinGW: an x86_64 toolchain).")
endif()

if(WIN32)
    enable_language(RC)
endif()

set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/..)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include(FetchContent)

# Flatbuffers
FetchContent_Declare(
  flatbuffers
  GIT_REPOSITORY https://github.com/google/flatbuffers.git
  GIT_TAG {{ .Deps.FlatBuffers }}
)
set(FLATBUFFERS_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(FLATBUFFERS_BUILD_SAMPLES OFF CACHE BOOL "" FORCE)
set(FLATBUFFERS_BUILD_FLATC OFF CACHE BOOL "" FORCE)
FetchContent_MakeAvailable(flatbuffers)

# SHM
# GIT_SUBMODULES "" disables submodule checkout: shm carries a benchmark-only
# submodule (benchmarks/external/aeron) that the interface library never needs,
# and whose .gitmodules entry is absent in released tags — recursing would fail
# the clone with "No url found for submodule path".
FetchContent_Declare(
  shm
  GIT_REPOSITORY https://github.com/xll-gen/shm.git
  GIT_TAG {{ .Deps.SHM }}
  GIT_SUBMODULES ""
)
FetchContent_MakeAvailable(shm)

# Types (Excel Types & Utils)
# EXCLUDE_FROM_ALL keeps types' test executables (tests/CMakeLists.txt defines
# ~10 of them unconditionally) out of the default build — only xll-gen-types,
# linked below, gets built. The EXCLUDE_FROM_ALL keyword on FetchContent_Declare
# (forwarded to add_subdirectory by MakeAvailable) requires CMake 3.28+, hence
# the minimum-version bump above. This replaces the older manual
# FetchContent_Populate + add_subdirectory dance, which CMake 3.30 deprecated.
FetchContent_Declare(
  types
  GIT_REPOSITORY https://github.com/xll-gen/types.git
  GIT_TAG {{ .Deps.Types }}
  EXCLUDE_FROM_ALL
)
FetchContent_MakeAvailable(types)

# Suppress cast-function-type warning in xll-gen-types (xlcall.cpp uses GetProcAddress with cast)
if(NOT MSVC AND TARGET xll-gen-types)
    target_compile_options(xll-gen-types PRIVATE -Wno-cast-function-type)
endif()

# Parallel Hashmap
FetchContent_Declare(
  phmap
  GIT_REPOSITORY https://github.com/greg7mdp/parallel-hashmap.git
  GIT_TAG {{ .Deps.PHMAP }}
)
set(PHMAP_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(PHMAP_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
FetchContent_MakeAvailable(phmap)

if(NOT TARGET shm)
  add_library(shm INTERFACE)
  target_include_directories(shm INTERFACE ${shm_SOURCE_DIR}/include)
endif()

{{ if eq .Build.Singlefile "xll" }}
# ==============================================================================
# Embedding Executable in XLL (exe_in_xll mode / singlefile: xll)
# ==============================================================================

FetchContent_Declare(
  zstd
  GIT_REPOSITORY https://github.com/facebook/zstd.git
  GIT_TAG {{ .Deps.Zstd }}
  SOURCE_SUBDIR build/cmake
)
set(ZSTD_BUILD_PROGRAMS OFF CACHE BOOL "" FORCE)
set(ZSTD_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(ZSTD_BUILD_SHARED OFF CACHE BOOL "" FORCE)
set(ZSTD_BUILD_STATIC ON CACHE BOOL "" FORCE)
set(ZSTD_LEGACY_SUPPORT OFF CACHE BOOL "" FORCE)

FetchContent_MakeAvailable(zstd)

add_executable(compressor "${CMAKE_CURRENT_SOURCE_DIR}/tools/compressor.cpp")
target_link_libraries(compressor PRIVATE libzstd_static)
target_include_directories(compressor PRIVATE ${zstd_SOURCE_DIR}/lib)

if(NOT DEFINED GO_SERVER_EXE_PATH)
    message(FATAL_ERROR "GO_SERVER_EXE_PATH must be defined via -DGO_SERVER_EXE_PATH=...")
endif()

# GO_SERVER_EXE_PATH is passed relative to the project root (where Taskfile is run).
# The project root is two levels up from CMAKE_CURRENT_SOURCE_DIR (generated/cpp).
get_filename_component(PROJECT_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../.." ABSOLUTE)
set(GO_EXE "${PROJECT_ROOT}/${GO_SERVER_EXE_PATH}")

if(NOT EXISTS "${GO_EXE}")
    message(FATAL_ERROR "Go binary not found at: ${GO_EXE}. Ensure it is built before running CMake.")
endif()

set(ZSTD_BINARY_NAME "go_server.zst")
set(ZSTD_OUTPUT_PATH "${CMAKE_CURRENT_BINARY_DIR}/${ZSTD_BINARY_NAME}")

add_custom_command(
    OUTPUT ${ZSTD_OUTPUT_PATH}
    COMMAND $<TARGET_FILE:compressor> ${GO_EXE} ${ZSTD_OUTPUT_PATH}
    DEPENDS compressor ${GO_EXE}
    COMMENT "Compressing Go binary with Zstd..."
)

if(WIN32)
    set(RC_FILE "${CMAKE_CURRENT_BINARY_DIR}/embedded_resource.rc")
    file(WRITE ${RC_FILE} "101 RCDATA \"${ZSTD_OUTPUT_PATH}\"")

    # Explicitly state that compiling the RC file depends on the ZST file being created.
    set_source_files_properties(${RC_FILE} PROPERTIES OBJECT_DEPENDS "${ZSTD_OUTPUT_PATH}")
endif()

{{ end }}

file(GLOB SOURCES
    "${CMAKE_CURRENT_SOURCE_DIR}/xll_main.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp"
)

{{ if eq .Build.Singlefile "xll" }}
if(WIN32)
    list(APPEND SOURCES ${RC_FILE})
endif()
{{ end }}

add_library(${PROJECT_NAME} SHARED ${SOURCES})

target_include_directories(${PROJECT_NAME} PRIVATE
  ${CMAKE_CURRENT_SOURCE_DIR}
  ${CMAKE_CURRENT_SOURCE_DIR}/include
)

target_link_libraries(${PROJECT_NAME} PRIVATE
  shm
  xll-gen-types
  flatbuffers
  phmap
)

if(WIN32)
    # COM libs required UNCONDITIONALLY by the always-on date auto-format module
    # (src/xll_date_format.cpp). It targets cells with the in-process Application
    # IDispatch route (AccessibleObjectFromWindow + late-bound Range.NumberFormat)
    # so it never changes the user's active selection. This route exists in every
    # build — sync-only projects with no RTD/ribbon still format dates — so these
    # libs must link regardless of the RTD/ribbon blocks below (which add them
    # again; duplicate entries are harmless to the linker).
    #   ole32/oleaut32 : COM + VARIANT/BSTR
    #   uuid           : IID_IDispatch and friends
    #   oleacc         : AccessibleObjectFromWindow(OBJID_NATIVEOM)
    target_link_libraries(${PROJECT_NAME} PRIVATE
        ole32
        oleaut32
        uuid
        oleacc
    )
endif()

{{ if .Rtd.Enabled }}
target_compile_definitions(${PROJECT_NAME} PRIVATE XLL_RTD_ENABLED)
if(WIN32)
    target_link_libraries(${PROJECT_NAME} PRIVATE
        ole32
        oleaut32
        uuid
{{- if .Rtd.ThrottleInterval }}
        # oleacc: AccessibleObjectFromWindow for the in-process Application
        # IDispatch route (rtd.throttle_interval sets RTD.ThrottleInterval).
        oleacc
{{- end }}
    )
endif()
{{ end }}

{{ if .Commands }}
# Command/ribbon IPC sender (xll::ribbon namespace) lives in src/ribbon_addin.cpp,
# already swept up by the file(GLOB SOURCES ... src/*.cpp) above. The RibbonAddIn
# COM class inside that TU compiles only when XLL_RIBBON_ENABLED is defined.
{{ if .Ribbon.Enabled }}
target_compile_definitions(${PROJECT_NAME} PRIVATE XLL_RIBBON_ENABLED)
if(WIN32)
    # ole32/oleaut32/uuid for COM; oleacc for AccessibleObjectFromWindow
    # (the in-process Application IDispatch route in ConnectRibbonAddin);
    # gdiplus for the ribbon image decoder TU (src/ribbon_image.cpp decodes
    # embedded PNG/JPG bytes via GDI+; ole32 already provides
    # CreateStreamOnHGlobal, so shlwapi is not needed).
    target_link_libraries(${PROJECT_NAME} PRIVATE
        ole32
        oleaut32
        uuid
        oleacc
        gdiplus
    )
endif()
{{ end }}
{{ end }}

{{ if eq .Build.Singlefile "xll" }}
target_link_libraries(${PROJECT_NAME} PRIVATE libzstd_static)
target_include_directories(${PROJECT_NAME} PRIVATE ${zstd_SOURCE_DIR}/lib)
target_compile_definitions(${PROJECT_NAME} PRIVATE EMBED_GO_SERVER)
{{ end }}

# Debug instrumentation switch. Declared with option() so it shows up as a
# typed BOOL in `cmake -LA` / CMakeCache.txt instead of an UNINITIALIZED entry.
#
# NOTE: option() only supplies the DEFAULT — it never overrides a value already
# in the cache. Release and Debug share one build tree (build/cpp), so the
# Release configure in Taskfile.yml passes -DXLL_DEBUG=OFF EXPLICITLY; that is
# the only thing that clears an ON left behind by an earlier debug configure.
# Do not drop either half.
option(XLL_DEBUG "Enable debug instrumentation (SHM_DEBUG + XLL_DEBUG_LOGGING)" OFF)

if(XLL_DEBUG)
  target_compile_definitions(${PROJECT_NAME} PRIVATE SHM_DEBUG XLL_DEBUG_LOGGING)
endif()

if(WIN32)
  # Prevent collision between shm/Logger.h (LogLevel::ERROR) and windows.h (ERROR macro)
  target_compile_definitions(${PROJECT_NAME} PRIVATE NOGDI)
endif()

if(MSVC)
  target_compile_options(${PROJECT_NAME} PRIVATE
    /utf-8
    $<$<CONFIG:Release>:/arch:AVX2>
  )
else()
  target_compile_options(${PROJECT_NAME} PRIVATE
    -fexceptions
    -fnon-call-exceptions
    -Wno-stringop-overread
    -Wno-pragma-once-outside-header
    $<$<CONFIG:Release>:-O3>
    $<$<CONFIG:Release>:-mavx2>
    $<$<CONFIG:Release>:-flto>
  )
  target_link_options(${PROJECT_NAME} PRIVATE
    # -static covers winpthread too; -static-libgcc/-static-libstdc++ alone
    # leave a libwinpthread-1.dll import that breaks machines without MinGW.
    -static
    -static-libgcc
    -static-libstdc++
    $<$<CONFIG:Release>:-flto>
    $<$<CONFIG:Release>:-s>
  )
endif()

set_target_properties(${PROJECT_NAME} PROPERTIES PREFIX "" SUFFIX ".xll")
