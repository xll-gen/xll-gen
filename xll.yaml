name: VerifyLogs
description: A project to verify logging behavior in singlefile mode
version: 0.1.0

build:
  singlefile: xll
  temp_dir: ${TEMP}

logging:
  level: debug
  path: server.log

functions:
  - name: Add
    args:
      - name: a
        type: float
      - name: b
        type: float
    return: float
    description: Adds two numbers
